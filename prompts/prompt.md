```markdown
промпт 1

помоги разработать телегармм бота который работает как простой AI-чат, используя локальную языковую модель (LLM). 
Функционал бота
1.принимает текстовые сообщения от пользователя
2.передает их в локальную LLM
3.возвращает сгенерированный ответ обратно в Telegram
4.сообщения не должны нигде сохраняться и обрабатываться независимо, как новый пустой чат
5.простота и чистота архитектуры 
дополнительно бот должен
• отвечать на любые текстовые сообщения
• корректно обрабатывать ошибки (например, если LLM недоступна)
• работать стабильно при нескольких запросах подряд
Технологии
язык - JavaScript 
LLM - gemma3:270m 
Telegram Bot API - важно! не используй webhook, только polling вот токен бота - 8661*****
Архитектура 
Telegram → Bot → LLM → Bot → Telegram 
создай package.json чтобы можно было запускать бота одной командой

промпт 2
давай дикомпозируем код по разным папкам и файлам, чтобы было понятно какой код что делает

промпт 3
давай добавим возможность выбирать модель в чате с ботом. вот вторая модель qwen3.5:0.8b

промпт 4
сделай чтобы при воде / появлялся селект с командами /start, /models
когда нажимаешь /models, то появляется список доступных моделей в виде кнопок
навзание будет в таком виде qwen 3.5
```
